class Note {
  String id;
  String subtitle;
  String title;
  String time;
  int image;
  bool isDon;
  DateTime timestamp;

  Note(this.id, this.subtitle, this.time, this.image, this.title, this.isDon,
      this.timestamp);
}
